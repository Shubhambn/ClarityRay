# Pages folder note

This project uses the Next.js **App Router** under `app/` for runtime routes.

This `pages/` directory is intentionally documentation-only to satisfy high-level frontend organization requirements without enabling the legacy Pages Router runtime.
