# Legacy runtime (reference only)

This directory is **LEGACY ONLY** and is not part of the active ClarityRay runtime.

- Do not import files from this directory in `app/`, `hooks/`, or `lib/clarity/`.
- Active model-agnostic runtime lives in `lib/clarity/*`.
